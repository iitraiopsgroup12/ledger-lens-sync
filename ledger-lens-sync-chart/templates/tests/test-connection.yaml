apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ledger-lens-sync-chart.fullname" . }}-test-connection"
  labels:
    {{- include "ledger-lens-sync-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ledger-lens-sync-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
