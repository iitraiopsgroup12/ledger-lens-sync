# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project state

This is a brand-new Python project scaffold with no implemented functionality yet — `main.py` is currently just a placeholder entry point. There is no architecture to document yet; update this file as real structure, modules, and commands are added.

## Commands

- Requires Python >=3.14 (see `.python-version`).
- Run the entry point: `python main.py`